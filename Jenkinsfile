
pipeline {
    //agent { label "${_AGENT_LABEL}" }
    stages {
      stage ('Initialize') {
        steps {
          echo 'Placeholder.'
          }
        }
    }
    post { always { echo "any post steps" } }
}
